<div class="engenharias-content">
  <% if notice %>
    <div class="alert alert-primary">
      <p id="notice"><%= notice %></p>
    </div>
  <% end %>
  <h1 class="title">Engenharias</h1>

  <table class="table table-hover">
    <thead class="header-table">
      <tr>
        <th>Descricao</th>
        <th>Status</th>
        <th>Usuario</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @engenharias.each do |engenharia| %>
        <tr>
          <td><%= engenharia.descricao %></td>
          <td><%= t engenharia.status %></td>
          <td><%= engenharia.usuario.nome %></td>
          <td><%= link_to 'Visualizar', engenharia %></td>
          <td><%= link_to 'Editar', edit_engenharia_path(engenharia) %></td>
          <td><%= link_to 'Excluir', engenharia, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Nova engenharia', new_engenharia_path, class: 'btn custom-primary-btn' %>
</div>
